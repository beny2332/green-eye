const router = require("express").Router()

// create a new poll
router.post('/', ()=>{})

router.get('/my', ()=>{})

router.get('/:id', ()=>{})

router.post('/reply', ()=>{})

router.delete('/:id', ()=>{})



module.exports =  router 
